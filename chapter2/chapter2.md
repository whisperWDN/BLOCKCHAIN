# 区块链数据层

## 区块头数据结构
- 版本号
- 前一区块
- 目标hash值
SHA256对区块头进行两次hash得到
- 随机数
- merkle树root
- 区块大小
- 时间戳

## 交易
- 生产交易
- 通用地址交易
- 合成交易

## tips
- 区块一般通过两种方式标识，区块的哈希值和区块高度
- 区块链中每一笔交易输出的币要么完全消费掉，要么完全不消费，不存在消费部分的情况
如果希望花费100个比特币中的50个，则比特币系统创建2个输出，第一个输出50比特币发往接收方地址，第二个发往发送方的某个地址



## 区块链的运行流程
1. 源节点创建交易，并验证目的节点的地址
2. 源节点对交易进行签名加密
3. 源节点将该交易广播至全网其他节点
4. 全网节点接收交易并验证其有效性，直到该交易被全网大多数节点验证和接收
5. 交易暂存于节点内存池，并判断是否为孤立交易
6. 交易被打包至本地节点区块中
7. 全网共识结束后，获胜节点将其本地区块追加到主链
8. 交易在主链上被越来越多的后续区块确认

## 网络传播与验证
### 交易池管理
内存池存储于本地内存中。
孤立交易：子交易会在父交易之前到达

### 交易优先级
取决于输入对应的UTXO的数量、创建时间和交易额的大小

## 关键技术
---
### 时间戳
假定存在全局时钟C
C0: 如果对于事件a和事件b，有a->b，那么C(a) < C(b)
C1: 如果事件a和事件b是同一个进程Pi中的事件，并且a在b之前发生，则有Ci(a) < Ci(b)
C2: 如果a是进程Pi上某消息发送事件，b是进程Pj上某消息接收事件，则有Ci(a) < Cj(b)

### 哈希函数

#### 特征
- 单向性
- 弱抗碰撞性
即给定输入数据x时，寻找其他数据x'!=x ，使得H(x)=H(x')在计算上是不可行的
- 强抗碰撞性
寻找两个不同的输入x'和x ，使得H(x)=H(x')在计算上是不可行的
- 谜题友好性
- 雪崩效应
- 定时定长性
不同长度输入数据的哈希过程消耗大约相同的时间产生固定长度的输出

#### md算法
1.填充数据
假设输入消息M的长度为b位，通过填充数据使其长度满足b mod 512 = 448.填充方法是在M之后的第一位填充1，其他位填充0.如果b=448，需填充512位，也就是说，必须填充
2.填充消息长度
将原始消息的长度表示为64位追加到消息末尾。如果超过2的64次方则取低64位.
3.初始化缓冲区
采用128位缓冲区(A、B、C、D四个32位缓冲区)
4.迭代消息处理
5.输出结果


#### SHA256算法
1.填充数据
假设输入消息M的长度为b位，通过填充数据使其长度满足b mod 512 = 448.填充方法是在M之后的第一位填充1，其他位填充0.如果b=448，需填充512位，也就是说，必须填充
2.填充消息长度
将原始消息的长度表示为64位追加到消息末尾。如果超过2的64次方则取低64位.
3.初始化缓冲区
采用256位缓冲区(8个32位缓冲区)
4.迭代消息处理
5.输出结果

#### Scrypt算法
内存困难型


#### 混合哈希算法

### 非对称加密算法
- RSA加密
- 椭圆曲线加密
- 离散对数加密